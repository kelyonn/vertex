# Project Vertex v4 — JARVIS Hologram Workbench
# Install with: pip install -r requirements.txt
# Note: PyOpenGL_accelerate intentionally omitted — fails to build on macOS SDK 26+

pygame==2.6.1
PyOpenGL==3.1.7
opencv-python==4.11.0.86
mediapipe==0.10.21
numpy>=1.21.2

# Voice control (optional — app degrades gracefully without these)
faster-whisper>=1.0.0
sounddevice>=0.4.4
openwakeword>=0.5.0
pyttsx3>=2.90
